1.
①要与串口通信首先要在项目添加RXTXcomm.jar包（放在项目中的lib目录下，并添加到build Path中，操作方法：项目结构中选择库，点击添加勾选）
②.需要将解压后的rxtxParallel.dll和rxtxSerial.dll两个文件放在%JAVA_HOME%/jre/bin目录下,这样该包才能被正常的加载和调用。
【在解压以后的包中有install.txt文档里面有需要放到的路径介绍（Copy RXTXcomm.jar ---> <JAVA_HOME>\jre\lib\ext  ；
	Copy rxtxSerial.dll ---><JAVA_HOME>\jre\bin；Copy rxtxParallel.dll ---> <JAVA_HOME>\jre\bin）】

2.
串口测试工具XCOM21安装时请记得关闭防火墙，并重置arduino

3.ContinueRead为JAVA调试代码块，实际功能性有待提高，先放实测的在这里（已经测试四版了，RXTX和comm太古老，import都有问题）
